## Ideas
- Summarize notes.
- Semantic search through jounals.
- AI scans notes for patterns and do some visual graphs or find behaviours.


### To do
- Feature to create note and folder using right click. Need to handle the actions appropriately.

#### Visual
- Update the dropdown icon location.
- Update the color of chat-popover-menu (to suit with the UI)
- Update the editor title padding and alignment with date.
- Make the save status dynamic.

#### Quality of Life
- Instead of creating a note and folder with default title/name. Create an interface to type the title/name, if it is empty then don't create a new record else create a new record.